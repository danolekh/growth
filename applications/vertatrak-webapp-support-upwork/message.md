# Upwork cover letter — VertaTrak

## Message (send this)

Your achievement-ledger requirement, an idempotent insert on (client_id, milestone_id) where the
UI celebration only fires after the completion write and the award insert both succeed, is close
to money-correctness work I did on a live iGaming platform: idempotent withdrawals keyed on a
ledger index, and single-statement bonus expiry with a debit written in the same statement. Same
instinct, source of truth over anything cached.

I'm a full-stack TypeScript developer, React and Postgres every day. Your stack is close to home:
React, TypeScript, Vite and Tailwind on the front, Vercel serverless API routes on the back,
Postgres underneath. I use Drizzle ORM daily and I'm comfortable working against raw SQL
migrations too, the schema thinking carries over either way. The server-side token verification
and tenant authorization you describe for practice-scoped routes reads like the per-brand access
rules I built recently: a shared decision function plus gateway-level checks, not just a
client-side gate. And deriving streaks from canonical daily history instead of a cached client
field is exactly that same ledger discipline again.

Honest about the gaps. I haven't used Firebase Auth or Firebase Cloud Messaging, and I have no PWA
or Capacitor experience, so the offline app-shell and native wrapper pieces would be new to me.
The web and API side would be a short ramp; Firebase and Capacitor would take longer, so I'd want
to start on something self-contained - the achievement ledger looks like a good first ticket - so
you can see the code before handing over anything bigger.

$32/hr, and glad to start with one small, well-defined ticket first so you can check the fit
before widening scope. Available now and can start today.

Portfolio: danolekh.com · GitHub: github.com/danolekh

## Form settings
- **Rate:** $32/hr (no budget shown in the post; mid of Dan's own $30-35/hr hourly ask, see
  `playbook/income-plan-2026-09.md`).
- **Resume:** `me/resume/out/Resume-fullstack.pdf`.
- **Timing:** posted 22 Sep, flagged **hot** by the watcher. Send today.
