# Zerion application — Senior Product Engineer, Frontend

English (remote web3 product company post).

## Message (send this)

"Sophisticated animations and interactive UI" is the line on your list I can point straight at
proof for. I built consolline.com solo in Astro this year: a WebGL2 globe that takes a push from
scroll and hands drag velocity back on release, a pointer-reactive halftone dot field, no
animation library, checked pixel-for-pixel against Figma across 14 routes and three breakpoints.

Owning a feature end-to-end is how I actually work. I built Quextro, an ed-tech platform, solo
from an empty repo to real users, now used by British teachers and students: React 19 and
TanStack on the front, Bun and Postgres with Drizzle on the back. On sportmagaz.com.ua I shipped
view transitions where the product photo morphs from the card into its page, the kind of
microinteraction detail your post cares about.

Since September I've been hands-on with EVM too. I shipped a USDC milestone escrow for Base in
Solidity and Foundry, deployed and verified on Base Sepolia (a testnet), 80 tests including fuzz
and invariant suites and fork tests against real USDC, all public:
github.com/danolekh/milestone-escrow. It's a bonus point on your list, and I want to be upfront
that it's new: no production contracts, no audits yet, no wallet-connected frontend built on it.

The honest gap is years: you ask for 5, I have 3 commercial. What I bring instead is the same
craft your post keeps circling back to, interaction and motion detail checked against the design
after it ships, not left to drift. I haven't owned a shared design system end to end either, just
a light Storybook setup on a side project. Claude Code is already central to my process, so the
AI-native workflow you describe is close to how things get built on my end.

Based in Vienna, EU time zone. Portfolio: danolekh.com · GitHub: github.com/danolekh (39 repos,
merged PRs to opentui and code-racer).

## Form settings
- **Salary expectations:** $3,500. No band stated, remote web3 product company, not Ukrainian, so
  the no-band EU/US default.
- **CV:** `me/resume/out/Resume-web3.pdf`
- **Note:** the live posting asks applicants to put the word WATERMELON in the answer to a
  screening question on the actual apply form ("we read every application in full..."). That
  question's text wasn't in the feed data, so it isn't answered here - check the real apply page
  for it before sending.
- **Timing:** feed post, no view/application counts available. Send today.
